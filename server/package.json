{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build:binary": "bun build --compile --outfile server src/index.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
